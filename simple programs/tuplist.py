# Program to count the number of students with the grade 'A' 
grades = ("C","D","A","A","B","B","A")
print("Number of students with grade 'A':",grades.count("A"))

# Program extends to store the same tuple in list (sorted form)
grades= list(grades)
grades.sort()
print(grades)